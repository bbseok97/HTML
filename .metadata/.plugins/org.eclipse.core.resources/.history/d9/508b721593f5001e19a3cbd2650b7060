<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>

<%
	// Servlet : 동적 웹 어플리케이션 (순수 자바 코드)
	// 클라이언트 요청에 동적으로 작동하고 , 응답은 html을 이용
	// java thread를 이용하여 동작
	// MVC 패턴에서 Controller로 이용됨.
	//	서블릿은 여러 개가 존재할 수 있다.
%>
<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>study03.jsp</title>
	</head>
	<body>
		<!-- URL Mapping -->
		<!-- URL 매핑을 통해 경로 노출을 없애고 보안을 강화하여 문제를 해결 -->
		<%--@webServlet() 형태로 선언 --%>
		
		<%
			// HttpRequest 방식 2가ㅣ
			// 1. Get 방식
			// 서버에 데이터를 요청하는 용도이며, 전송하는 데이터가 주소에 묻어간다.
			// 전송한 데이터는 브라우저 히스토리에 접속했던 주소와 함께 남아있어 보안에 취약
			// 게시판 글 조회, 검색 등과 같이 서버의 정보를 가져올 떄 사용.
			// 전송할 수 있는 최대 크기는 브라우저 별로 다르나 크기가 정해져 있음.
			// HTML form 태그가 반드시 필요하지 않다.
		%>
		<%
			// 2. Post 방식
			// 서버에 데이터를 전송하는 용도.
			// 전송된 데이터가 URL에 묻어나가지 않고, 전송 객체의 메시지 바디를 통해 전달
			//  (보안성에 강하다.)
			// private한 데이터 전송에 사용
			//	HTML form 태그가 필요하다.
			//	대량의 데이터 전송이 가능하다.(데이터 양의 제한이 없음)
		%>
	</body>
</html>